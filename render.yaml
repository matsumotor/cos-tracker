services:
  - type: web
    name: cos-tracker
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: AUTH_TOKEN
        generateValue: true     # Render gera token aleatorio na 1a deploy
      - key: NODE_VERSION
        value: 20
